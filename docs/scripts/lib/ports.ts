/** Local dev ports — keep in sync with encore.app CORS and frontend dev script */
export const FRONTEND_DEV_PORT = 5000;
export const BACKEND_DEV_PORT = 8000;
export const BACKEND_DEV_URL = `http://localhost:${BACKEND_DEV_PORT}`;
